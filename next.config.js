/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Add the following line to specify the output directory
  distDir: ".next", //your publish directory is '.next'
}

module.exports = nextConfig;